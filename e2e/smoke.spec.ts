// NC-6B TEMPORARY: the spec file still exists, so the harness precondition
// check passes, but it declares no test. Playwright must therefore collect
// zero tests and fail closed on its own rather than reporting success.
// Reverted after this run.
