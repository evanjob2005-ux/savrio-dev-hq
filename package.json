{
  "name": "savrio-dev-hq",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "trigger:dev": "trigger dev",
    "test": "vitest run",
    "test:node": "vitest run --project node",
    "test:dom": "vitest run --project dom",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@trigger.dev/sdk": "4.5.7",
    "next": "16.2.11",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@tailwindcss/postcss": "^4",
    "@testing-library/react": "^16.3.2",
    "@trigger.dev/build": "4.5.7",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.11",
    "jsdom": "^29.1.1",
    "tailwindcss": "^4",
    "trigger.dev": "4.5.7",
    "typescript": "^5",
    "vitest": "^4.1.10"
  },
  "//overrides": {
    "why": "Security overrides. Every entry resolves a high or critical advisory whose parent pins a vulnerable range. None is a downgrade and none changes a direct dependency. Verified: npm audit reports 0 high and 0 critical at both full and --omit=dev scope. Remove an entry only when its parent's own range has moved past the advisory, and re-run npm audit at both scopes to confirm.",
    "load-bearing": "postcss, sharp, ws, brace-expansion, minimatch, tar - each is the only thing holding its dependents off a vulnerable version.",
    "redundant-but-kept": "engine.io - socket.io@4.8.3 already requires ~6.6.0, so this pin adds nothing today. Kept as belt-and-braces; safe to drop. Do NOT drop ws on the same reasoning: it is load-bearing on the engine.io-client path that the Trigger.dev realtime SDK actually uses.",
    "riskiest": "minimatch 3.x -> 10.x crosses seven majors across the ESLint toolchain (export shape changed at v5). Verified by linting 175 files with 0 errors. Re-run npm run lint after any change here."
  },
  "overrides": {
    "postcss": "^8.5.18",
    "sharp": "^0.35.0",
    "socket.io": "^4.8.3",
    "engine.io": "^6.6.9",
    "ws": "^8.21.1",
    "brace-expansion": "^5.0.8",
    "minimatch": "^10.2.6",
    "tar": "^7.5.13"
  }
}
