# Open Obligations Register

**Document ID:** OBL-001
**Created:** 2026-07-28
**Status:** Active
**Owner:** Founder (reassignment permitted)

---

## Purpose

Work that is known, accepted, and **not yet done**. Every item here was
identified by a named review or verification step and deliberately deferred
rather than dropped. Nothing in this register is a defect in working behaviour;
each is a gap between what a control claims and what it currently enforces, or a
follow-up whose right time has not arrived.

This register exists because the alternative is a review finding living only in
a pull request comment, where it is invisible six weeks later. An item leaves
this file when it is done and its proof is recorded, not when it stops being
mentioned.

**Ordering is by consequence, not by severity label.** The first two items
determine whether the security gates keep meaning what they say.

---

## Repository and platform settings

| ID | Obligation | Why it matters | Proof of completion |
|---|---|---|---|
| **OBL-01** | Raise `required_approving_review_count` back to **1** in the `Sprint 1F active line protection` ruleset once a second reviewer exists. | Lowered to 0 on 2026-07-28 because the single collaborator is also the author of every pull request, and GitHub does not permit self-approval — the requirement was unsatisfiable, not protective. At 0, nothing human gates a merge. | Ruleset shows `required_approving_review_count: 1` and a merged pull request carries an approval from someone other than its author. |
| **OBL-02** | Re-enable Renovate `automerge` for non-major devDependencies **only after** OBL-01. | Currently `false`. With a 0-approval gate on a public repository, auto-merge would put a compromised build-tooling release into the default branch unattended. | `renovate.json` sets `automerge: true` and the ruleset requires a review. |
| **OBL-04** | Enable code-owner review enforcement in the ruleset. | `.github/CODEOWNERS` assigns `@evanjob2005-ux` to 31 paths, but assignment is not enforcement. Until this is on, the protected files named in Binding Operating Kernel rule 9 are not actually gated. | Ruleset shows `require_code_owner_review: true`. |
| **OBL-34** | Create the **`production-release`** environment in repository settings, with a required reviewer who is not the release author. | `.github/workflows/release.yml:679` declares `environment: production-release` on the `publish` job, and the workflow's entire approval model rests on it — the comment above it at `:677-678` reads *"Protected environment: human approval and self-review prevention."* **The environment does not exist.** `gh api repos/:owner/:repo/environments` returns `{"total_count":0,"environments":[]}`, and GitHub applies no protection rule for an environment name it does not know, so there is today no required reviewer, no approval wait, and no self-review prevention on the publishing job. The P0-11 revalidation landed in `65bdf2a` and extended in `7972df4` — publish re-collects and re-evaluates the required checks on the far side of the gate against a byte-identical verifier — is correct and **inert**, because there is no approval wait for anything to change during. No workaround was added and none should be: a workflow cannot create its own protection. **This is a repository setting and therefore Founder-owned**, the same class as OBL-01 and OBL-04. | `gh api repos/:owner/:repo/environments` reports `production-release` with at least one required reviewer, and a publish run is observed waiting on that approval. |
| **OBL-29** | Reconcile `release.yml`'s `REQUIRED_CHECKS` list with the branch ruleset's required status checks, and decide which is authoritative. | Fixing CI-07 on 2026-07-29 required naming the checks a release is gated on. That list now lives in `.github/workflows/release.yml` (`verify-checks.env.REQUIRED_CHECKS`, 16 names) because a workflow cannot read the repository's ruleset. **Two lists can now disagree**: a check required by the ruleset but absent from this list would not block a release, and vice versa. The list fails closed on a rename — a renamed job reads as MISSING — so drift is loud rather than silent, but it is still drift. Ruleset content is a repository setting and a Founder-owned decision. | The ruleset's required status checks and `REQUIRED_CHECKS` name the same set, or one is recorded as deriving from the other. |

---

## Security controls — the two that matter most

| ID | Obligation | Why it matters | Proof of completion |
|---|---|---|---|
| **OBL-05** (RR-05) | The guard rule's message claims `rejectInternalDevRequest` must be called **as the first statement**; the rule does not enforce ordering. Each `pattern-not` opens with `...`, so a handler doing unauthenticated work *before* the guard still matches the exclusion and is not reported. | A route could delete data before authenticating and pass the gate that exists to prevent exactly that. No fixture covers this class, so the positive control would not catch a regression either. | A fifth fixture with pre-guard work, detected by the rule; or the message softened to match what is enforced. **⚠ This condition now appears to be met and the row has NOT been moved.** Found 2026-07-29 while recording other obligations, outside that pass's scope to close. Evidence, by reading at `638e45c`: `647701f` removed the leading `...` from the `pattern-not` blocks and `.semgrep/dev-hq-boundaries.yml:96-101` now states the requirement it rests on; `.semgrep/fixtures/route-guard-after-write.ts` is the pre-guard-work fixture this row asks for, and `508609c` added a second (`route-guard-not-enforced.ts`) for a write placed *between* the binding and the return; `.github/workflows/security.yml:1274` carries `internal/guardafter/route.ts` in the positive control's known-bad set against the guard rule. **Not verified by executing Semgrep**, which is what closing this row should rest on. Left open deliberately — closing an obligation as a side effect of annotating a different one is how this register acquired false claims twice. |

---

## Execution lifecycle

Recorded 2026-07-29 from the sixteen-commit remediation range `0d83525..638e45c`. Each was
found by a named review or disclosed by the commit that opened it, and each was deliberately
left rather than fixed under a task scoped elsewhere.

| ID | Obligation | Why it matters | Proof of completion |
|---|---|---|---|
| **OBL-35** | Before adding any assignment-release path that can leave or return an execution queued and unassigned, add `Execution.queuedSince` or prove that path preserves the derived anchor. | The anchor remains derived from execution and assignment timestamps. Four release writes exist; only three can remain observable to nonterminal queued work, and all three currently represent a legitimate end of unassigned stranding. `cancelExecution` is terminal and irrelevant to stall detection. The code comment now records the exact four/three distinction. This is a contingent coupling risk, not a currently manifested defect; adding an optional field with fallback would preserve the coupling and would not close it. | `Execution.queuedSince` is a maintained invariant read directly by stall detection, or every new qualifying release path has a red/null test proving the deadline remains anchored correctly. |

---

## Build and test infrastructure

| ID | Obligation | Why it matters | Proof of completion |
|---|---|---|---|
| **OBL-20** | Add a runtime smoke test for the Trigger.dev realtime transport. **Close before deploying, not merely before merging.** | Review traced the reachable path more precisely than first recorded: the `socket.io` and `engine.io` *server* packages are provably never imported by this application (0 imports against a positive control finding 6 `socket.io-client` imports). The live path is `@trigger.dev/sdk` → `socket.io-client` → `engine.io-client` → **`ws` 8.17.1 → 8.21.1**, and that `ws` bump is the actual exposure. Narrower than feared, but still unproven: nothing in 392 unit tests, the build, or 8 e2e tests opens a realtime connection, so realtime is working-by-assumption. | A test that establishes a Trigger.dev realtime subscription, **asserts the transport negotiated to `websocket`** (a long-polling fallback would exercise `xmlhttprequest-ssl` and prove nothing), and receives an event. |
| **OBL-25** | Re-attempt **ESLint 10** when `eslint-config-next` ships a compatible plugin set. | Re-attempted 2026-07-29 in an isolated worktree from `5bb0a42` with the then-latest `eslint@10.8.0` and `eslint-config-next@16.2.12`. `npm ci`, TypeScript 5.9.3, 802 tests, and the production build passed, but `npm run lint` exited 1: bundled `eslint-plugin-react@7.37.5` crashed in `react/display-name` with `TypeError: contextOrFilename.getFilename is not a function`. The config advertises `eslint >=9`, while its bundled `eslint-plugin-import@2.32.0`, `eslint-plugin-jsx-a11y@6.10.2`, and `eslint-plugin-react@7.37.5` peer ranges stop at ESLint 9; `npm ls` therefore reports ESLint 10 invalid. The override set cannot be declared safely reduced while the candidate cannot lint. | From a clean install, `npm ci`, `tsc --noEmit --incremental false`, the full test suite, `npm run lint`, and `npm run build` all exit 0 on ESLint 10; the resolved plugin tree accepts ESLint 10 without invalid peers; any override reduction is separately load/lint tested; and the repository's full and production-only npm audit policy checks are re-proved against the regenerated lockfile. |
| **OBL-26** | Re-attempt **TypeScript 7** when `typescript-eslint` supports it. | Re-attempted 2026-07-29 in a separate isolated worktree from `5bb0a42` with `typescript@7.0.2` and current `typescript-eslint@8.65.0`. `tsc --noEmit --incremental false` and all 802 tests passed. Lint failed with the package's explicit `typescript-eslint does not support TS 7.0` guard; its declared TypeScript peer range remains `>=4.8.4 <6.1.0`. The Next production build also failed: after detecting TypeScript as unsupported/missing and attempting its dependency-repair path, the build worker ended with `The "id" argument must be of type string. Received undefined`. Tracking remains typescript-eslint#10940. | From a clean install, `npm ci`, `npm run lint`, `tsc --noEmit --incremental false`, the full test suite, and `npm run build` all exit 0 under TypeScript 7 without peer overrides or an install during the build; and the repository's full and production-only npm audit policy checks are re-proved against the regenerated lockfile. |
| **OBL-11** | Close the Mission Control exit-gate evidence gap. | `proxy.ts` fails the whole `/api/dev-hq/*` surface closed in production and the e2e harness serves a production build, so **live progress, approvals, and blockers are never exercised**. The gate is partially evidenced: shell, layout, and viewport only. | E2E covering approval and blocker paths against a live data layer. |
| **OBL-30** | Wire `scripts/verify-workflow-structure.py` into CI so silently deleted workflow controls fail a build. | **PARTIAL CANDIDATE.** `lint.yml` runs the verifier against the triggering raw base SHA and runs its matrix/null/approval harness against the same SHA on push, pull request, and manual dispatch. The verifier compares the union of structural keys plus explicit workflow/job/steps/step container identities; typed presence records prevent a literal value from colliding with a missing field. Each intentional-change record binds the resolved base commit and workflow tree, candidate workflow-tree digest, changed-file set, canonical diff digest, and rationale; the fingerprint covers every field. Separate records cover the actual PR target, branch push-before, and manual-dispatch installation bases, while later no-workflow-change pushes ignore historical records. SHA-256 provides tamper evidence only: it is not a signature and does not prove human review because any repository writer can regenerate it. Requiring the ordinary `Lint / GitHub Actions` job name does not supply the missing trust anchor: candidate code can preserve that green name while hollowing/removing the workflow invocation, verifier, harness, or manifest. No external enforcement capability is asserted to exist yet. | Establish a trust anchor outside candidate-controlled files: either a supported and verified repository/organization required workflow or ruleset whose implementation is protected, or independently enforced path ownership/review for `lint.yml`, `scripts/verify-workflow-structure.py`, `scripts/test-verify-workflow-structure.py`, and `scripts/workflow-structure-approval.json`. Then observe a mutation proving that implementation or invocation hollowing cannot preserve the required green check. |

---

## Provenance and governance

| ID | Obligation | Why it matters | Proof of completion |
|---|---|---|---|
| **OBL-12** | Re-save `Viybd_HQ_Master_Roadmap_v10.4_Binding_Operating_Kernel.docx` to include the eight repository deltas, and refresh its stale `dc:title`/`dc:description` (still say v10.3). | The repository file and the `.docx` differ by the kernel preamble insertion and amendments A-1 through A-7. Until the source is re-saved, the "the `.docx` governs" rule is suspended for those eight deltas. **The count is authoritative in `docs/roadmap/MASTER_ROADMAP.md` §Consequence for provenance, not here** -- A-6 and A-7 were added after this row was first written, and re-saving against a stale count would drop them from the governing document while this register read closed. | Source hash re-registered with no suspension clause, and the delta count in this row reconciled against the roadmap immediately before the re-save. |

---

## Process

| ID | Obligation | Why it matters | Proof of completion |
|---|---|---|---|
| **OBL-18** | Reduce planning-artifact volume per sprint. | Sprint 1F carries **7,529 lines across ten planning documents** for work not yet shipped, including separate advisory, decision, and reconciled-decision records for a single track. Roadmap §9 now requires gates be proved by executable checks rather than prose; the artifact count has not yet followed. | A sprint completed with one entry package, one decision record, one validation report. |

---

## Closed

| ID | Obligation | Closed | Evidence |
|---|---|---|---|
| OBL-39 | Remove inert assignee conjunct from `listReadyWork` | 2026-07-29 | Readiness now filters only active status. The adapter test proves active records remain listed regardless of the historical assignee field while inactive records remain excluded; restoring the conjunct makes the owned-record assertion fail. |
| OBL-40 | Distinguish absence from unreachable remote at three release tag probes | 2026-07-29 | All three capture `git ls-remote`, emit a site-specific `::error::`, and exit 2 on failure. Real Git-for-Windows Bash executed two reachable exit-0 null arms, the recheck's reachable expected-mismatch evaluation arm (exit 1), three unreachable red arms, and three independent mutations removing each site's branch; all nine site-specific expectations passed. P0-10 separately supplies the recheck's genuine stable exit-0 null. The full legacy harness reached 82/90 because eight separate Python-to-`gh` stub cases hit host WinError 1312; those failures are outside these shell controls and are disclosed, not counted as OBL-40 evidence. |
| OBL-14 | Apply `claude-design` → `design-engineer` on CPU-001 revision | 2026-07-29 | CPU-001 was revised with both live references changed in the same pass. |
| OBL-15 | Decide roadmap-kernel authority | 2026-07-29 | ACR-001 X-8/X-27 records that the roadmap and kernel remain outside and are not promoted into the eight-tier hierarchy; they supplement planning, cannot override it, and conflicts escalate. |
| OBL-16 | Decide Savrio → Viybd rename | 2026-07-29 | ACR-001 X-28 retains Savrio/Dev HQ and treats Viybd as a roadmap source/historical label pending any separately commissioned rebrand. |
| OBL-19 | Resolve nine missing role handbooks | 2026-07-29 | Nine referenced handbooks now exist and derive from their role definitions and higher governance. This closure decides only reference completeness; `employees/**` remains active/supporting and any source conflict follows the hierarchy and escalation rules, including open X-7 where applicable. `verify-agent-references.py` resolves and structurally validates all 19. |
| OBL-09 | Regenerate and validate `package-lock.json` | 2026-07-29 | Node 24.18.0's bundled npm 11.16.0 regenerated the lockfile byte-for-byte identically: no package resolution or dependency version changed. All four global npm replacement steps were removed from the three workflows, and a clean registry-backed `npm ci` succeeded using bundled npm. Re-proof after installation: plain full-scope `npm audit --json` reported **2 moderate, 0 high, 0 critical** and the scanner exited 1 because findings exist; the repository policy parser passed because high/critical are zero. `npm audit --omit=dev --json` reported **0 total**, scanner exit 0, and policy pass. This does not claim the all-scope tree has zero findings. |
| OBL-24 | Re-prove audit results after lockfile regeneration | 2026-07-29 | The audit was rerun rather than inferred from the byte-identical lockfile. Full-scope `npm audit --json`: 2 moderate, 0 high, 0 critical, scanner exit 1; the repository policy step passed because only high/critical block. Production `npm audit --omit=dev --json`: 0 total, scanner exit 0, policy pass. The overridden packages retained their intended resolved versions, their load checks passed, and lint, type-check, 802 tests, and the production build passed. |
| OBL-27 | Reconsider the npm 11.16.0 pin now that CI runs Node 24 | 2026-07-29 | All four pins were removed, including both independent `frontend-tests.yml` jobs. The workflows now record the setup-node-provided Node/npm versions instead of replacing npm globally. A clean registry-backed `npm ci` succeeded with Node 24.18.0's bundled npm 11.16.0. |
| OBL-31 | Resolve three missing standards | 2026-07-29 | Naming, logging, and error-handling standards were authored without removing agent requirements; the reference verifier and red/null harness cover them. |
| OBL-36 | Define all escalation-resolution verbs against linked non-terminal work | 2026-07-29 | Founder authorized all eight section-3 decisions with “do these, all of them.” ADR-0003 records accept/abandon/revise semantics. `stall-resolution-lifecycle.test.ts` covers queued and running for all verbs; `escalation-decision-recency.test.ts` covers replay and supersession. |
| OBL-37 | Decide the unused `claimTask` port member | 2026-07-29 | ADR-0003 D6 chooses removal. `claimTask` is absent from `types/contracts/task-repository.ts` and `lib/dev-hq/adapters/dev-task-repository.ts`; TypeScript validates the composition root and adapter against the reduced port. |
| OBL-38 | Complete an agent task at its authorized success gate | 2026-07-29 | `task-completion-service.ts` writes through `updateTaskStatusIf`: no-review success completes immediately, basic/full only after pass, and stale/blocked outcomes refuse. Isolated guard tests plus service-path replay tests pass. |
| OBL-32 | Tracked Python bytecode under `scripts/__pycache__/` | 2026-07-29 | Untracked in `8468d17`, the same commit that fixed the verifier whose harness produced it. **`git ls-files scripts/__pycache__/` returns nothing** (no output, exit 0), which is this row's stated proof. `git log --diff-filter=D -- 'scripts/__pycache__/*'` names `8468d17` as the deleting commit. The ignore half holds independently: `git check-ignore -v scripts/__pycache__/foo.pyc` reports `.gitignore:195:__pycache__/`, so a regenerated file cannot be re-added by accident. |
| OBL-33 | Two in-code records contradicted verified repository state | 2026-07-29 | Both corrected, in **different** commits. **(a)** `647701f` — `eslint.config.mjs:30-32` now reads *"The tsc side is now explicit too -- tsconfig.json:33 carries `"exclude": ["node_modules", ".semgrep/fixtures"]` -- which closed OBL-08"*, and `tsconfig.json:33` is exactly that line. The same edit removed the stale lintable-file count rather than restating it, so that sentence cannot go stale a second time. **(b)** `d7e9c97` — `e2e/mission-control-viewport.spec.ts:120-124` now states the scope limit (*"under this build proxy.ts 403s the whole Dev HQ surface and MissionControlOverview.tsx:94 returns the loading placeholder, so what this measures is the shell, not the populated view"*) and points at `OBL-11`. Neither file now cites a closed obligation as open. **Provenance correction:** `e318491`'s message claims the e2e comment *"is corrected here"*. It was not — `git log -S "See OBL-11" -- e2e/mission-control-viewport.spec.ts` names `d7e9c97`, the commit immediately before it. The correction is real; the commit that claims it is wrong. |
| OBL-03 | 42 npm advisories, 20 high/critical, 7 in production | 2026-07-28 | Resolved by eight `overrides`; 0 high / 0 critical at both scopes, no downgrades, no direct dependency changed. **Correction retained:** the first attempt claimed the fix was upstream-only and proposed accepting all 20 for 90 days — that claim was false and independent review disproved it by construction. |
| OBL-22 | Overrides had no exit path | 2026-07-28 | Overrides parent-scoped and folded into their parents' Renovate groups; a dedicated `matchDepTypes: ["overrides"]` rule labels override updates so accumulation is visible. Removal rule recorded in the `//overrides` note. |
| OBL-23 | Whole-tree audit did not run on pull requests | 2026-07-28 | `dependencies.yml` no longer skips `npm-audit` on `pull_request`; the proof is now bound to the candidate rather than running only after merge. |
| OBL-06 | `.env` credential name-coverage gaps | 2026-07-28 | Alternation widened with SIGNING, WEBHOOK, BEARER, SESSION, SALT, PRIVATE, ACCESS, REFRESH, APIKEY, PWD. Verified: 12 real-credential shapes caught, 5 legitimate values ignored, 0 findings across 382 tracked files. |
| OBL-07 | `scanner:allow-secret` pragma undocumented | 2026-07-28 | Documented in SECURITY.md with the rationale (a regex edit silently weakens the rule for everyone; a pragma is a reviewable line with an author). Verified all 6 documented behaviours, including both worked examples. |
| OBL-08 | tsc exclusion of the Semgrep fixtures was incidental | 2026-07-28 | Explicit `exclude` added to tsconfig.json. Proven load-bearing by the rename scenario it guards: a non-dotted fixtures directory IS type-checked without it and is not with it. |
| OBL-10 | `waitForLoadState("networkidle")` in the e2e suite | 2026-07-28 | Both waits replaced with web-first assertions on the banner landmark. 8 e2e tests pass across both viewports. |
| OBL-13 | No CI check that the roadmap matches its registered hash | 2026-07-28 | `scripts/roadmap-conversion/verify-registered-hash.py`, wired into ci.yml. Verified 3 ways: passes on the real file, exits 1 on a tampered body, exits 2 (not 0) when the boundary marker is missing. |
| OBL-17 | Roadmap conversion unreproducible | 2026-07-28 | Conversion and fidelity scripts preserved under scripts/roadmap-conversion/. **The script reproduces `1720060d…92b46` exactly** — the as-converted hash the registration record states, which independent review had recorded as an unverifiable claim. |
| OBL-21 | 13 production advisories from one OTel CVE | 2026-07-28 | `@opentelemetry/core` overridden to ^2.8.0. **Production advisories 13 → 0**, all scopes 19 → 2. Verified all 8 OTel entry points resolve, the Trigger.dev SDK loads, and lint/tsc/392 tests/build/8 e2e pass. |
| OBL-28 | No detection for silently deleted CI guards | ~~2026-07-28~~ **CLOSURE RETRACTED 2026-07-29** | `scripts/verify-workflow-structure.py` was rewritten and wired to `scripts/test-verify-workflow-structure.py`. **10 of 10 mutations caught**, against 3 of 10 for the removed version — that part is real and stands. **But the verifier runs in no workflow.** `grep -rn "verify-workflow-structure" .github/` returns nothing (exit 1); the only references anywhere are the script, its own test harness, and this register. A script wired to its test harness is not a CI gate, so the gap this row claimed to close — *silently deleted CI guards go undetected* — is still open. Reopened as **OBL-30**. Found independently by three reviewers (DOC-01 / CI-04 / CI-11). |
| CR-01 | Credential scanner filtered by file kind | 2026-07-28 | Rewritten to judge value shape; 8 constructed bypass cases detected, 0 findings across 381 files |
| CR-02 | `.env` template widening left unquoted secrets unflagged | 2026-07-28 | Unquoted-assignment rule added; real value caught, empty and placeholder exempt |
| CR-03 | Renovate auto-merge unsafe | 2026-07-28 | `automerge: false`; carried forward as OBL-02 |
| CR-05 | Provenance record contradicted its own delta set | 2026-07-28 | Corrected to five amendments / six deltas; body hash re-derived independently |
| CR-06 | Guard rule had three false-negative classes | 2026-07-28 | All four handler shapes matched, bind-and-return enforced, positive control asserts fixture-to-rule mapping |
| CR-07 | E2E discarded every resource-load failure | 2026-07-28 | Structured response events; proven non-vacuous with a negative control |
| CR-08 | Rename left dead paths in forward-looking documents | 2026-07-28 | 30 → 7 references; remainder in dated evidence records only |
| CR-09/10/11 | Semgrep unpinned, unanchored, mislabelled severity | 2026-07-28 | Pinned to 1.145.0, patterns anchored, severity raised to ERROR |
| RR-01 | Placeholder prefix-anchoring exempted real credentials | 2026-07-28 | Prefix now paired with shape and entropy tests; four reviewer bypass cases detected |
| RR-04 | Positive control asserted a count, not a mapping | 2026-07-28 | Asserts the exact `(path, rule)` pair set |
| RR-06 | Console listener removed, trading one blind spot for another | 2026-07-28 | Restored unfiltered alongside structural response assertions |
